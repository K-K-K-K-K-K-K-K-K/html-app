name: git-workshop-demo

on: 
    push:
        branches:
            - 'feature/**'
            - 'bugfix/**'
            - 'chore/**'

jobs:
  job-1:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello GitHub Action 1"
  job-2:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello GitHub Action 2"